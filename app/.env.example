# Copy to .env and fill in from Supabase project settings -> API.
# Both are safe to ship in the client bundle -- see lib/supabase.ts and
# docs/screen-data-contract.md for why the anon key alone is sufficient.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
